apply plugin: 'com.android.library'
apply plugin: 'com.github.gmazzo.library-apk'

android {
    compileSdkVersion 27

    defaultConfig {
        minSdkVersion 16
        targetSdkVersion 27

        versionCode 1
        versionName '1.0'
    }
}
